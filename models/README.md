# Models

This folder stores pretrained or fine-tuned model weights.

Expected files:
- hand_gesture_model.pth
- facial_expression_model.pth

These weights will be used once training is completed on the custom dataset and referenced by the production inference pipeline.
